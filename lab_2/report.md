# Отчет по лабораторной работе №2

## Задание
Написать программу на языке C/C++ для перемножения двух матриц с использованием OpenMP для параллельных вычислений.
Исходные данные: файл(ы) содержащие значения исходных матриц.
Выходные данные: файл со значениями результирующей матрицы, время выполнения, объем задачи.
Обязательна автоматизированная верификация результатов вычислений с помощью сторонних библиотек или стороннего ПО (например на Matlab/Python).

## Реализация
На языке C++ написан класс `Matrix`, реализующий хранение и операции над матрицами, включая их умножение. Программа `main.exe` выполняет генерацию матриц, их запись в файлы, перемножение с использованием OpenMP и сохранение результатов.

### Алгоритм работы программы:
1. Генерируются две квадратные матрицы размером от 100x100 до 1000x1000 со случайными значениями в заданном диапазоне.
2. Матрицы сохраняются в файлы `aN.txt` и `bN.txt`, где `N` — размерность.
3. Производится умножение матриц с использованием OpenMP, замеряется время выполнения.
4. Результат записывается в файл `resultN.txt`.
5. Среднее время выполнения рассчитывается по 10 запускам для каждой размерности.
6. Верификация результатов выполняется с использованием Python и библиотеки `numpy`, где сравниваются файлы `resultN.txt` с результатами, полученными через `numpy.dot()`.

### Использование OpenMP:
Для распараллеливания умножения матриц были использованы:
- `#pragma omp parallel` для создания параллельных потоков.
- `#pragma omp for` для распределения итераций внешнего цикла по потокам.
- `#pragma omp simd reduction(+:sum)` для оптимизации вычисления скалярного произведения строк и столбцов.
- Использовано 4 и 12 потоков для сравнения эффективности.

## Результаты
Замерены средние времена выполнения при различном числе потоков:

| Размер | Время (без параллелизма) | Время (4 потока) | Время (12 потоков) |
|--------|--------------------------|------------------|--------------------|
| 100    | 0,0108 с                 | 0,00348 с        | 0,00283 с         |
| 200    | 0,1135 с                 | 0,01915 с        | 0,01414 с         |
| 300    | 0,3067 с                 | 0,05570 с        | 0,03728 с         |
| 400    | 0,6762 с                 | 0,13741 с        | 0,08366 с         |
| 500    | 1,3357 с                 | 0,26933 с        | 0,15757 с         |
| 600    | 2,3715 с                 | 0,44608 с        | 0,26515 с         |
| 700    | 3,7705 с                 | 0,73357 с        | 0,41586 с         |
| 800    | 5,6101 с                 | 1,0708 с         | 0,62167 с         |
| 900    | 8,1979 с                 | 1,5233 с         | 0,90503 с         |
| 1000   | 10,8303 с                | 2,0294 с         | 1,2244 с          |

![График](https://github.com/Darowiin/ParallelProgramming/blob/main/lab_2/image.png)

## Вывод
Добавление OpenMP позволило значительно ускорить вычисления. Использование 4 потоков дало ускорение примерно в 4-5 раз, а 12 потоков — до 8-9 раз по сравнению с последовательным алгоритмом. Однако прирост производительности не является линейным из-за накладных расходов на управление потоками и возможных конфликтов доступа к памяти.

